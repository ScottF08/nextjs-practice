/**
 * Pages are a different way to have components and routing. The routing is based on folder structure. Each folder will have page.tsx for that specific
 * route endpoint.
 */

export default function Page() {
  return <p>Dashboard Page</p>;
}